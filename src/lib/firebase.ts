// Re-exports google-auth (GIS-based) — kept for backward compat with useTarefas
export { googleSignIn, getAccessToken, logout, initAuth } from "./google-auth";
export type { GoogleUser } from "./google-auth";
